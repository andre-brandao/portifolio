---
import { Image } from "astro:assets";
import type { CardProps } from "./types";

interface Props extends CardProps {}
const { title, img, desc, href, badge, tags } = Astro.props;
---

<a href={href} class="card-link">
  <article class="card-h">
    {
      img && (
        <div class="card-h-media">
          <Image
            src={img}
            width={750}
            height={422}
            format="webp"
            alt={title}
            class="card-h-img"
            transition:name={title + "image"}
          />
        </div>
      )
    }

    <div class="card-h-body">
      <header class="card-h-head">
        <h3 class="card-h-title" transition:name={title}>
          {title}
        </h3>
        {badge && <span class="card-h-badge">{badge}</span>}
      </header>
      <p class="card-h-desc" transition:name={desc}>{desc}</p>
      {
        tags && tags.length > 0 && (
          <ul class="card-h-tags">
            {tags.map((tag) => (
              <li>{tag}</li>
            ))}
          </ul>
        )
      }
    </div>
  </article>
</a>

<style>
  .card-link {
    display: block;
    text-decoration: none;
    color: inherit;
  }
  .card-h {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    padding: 1rem;
    border-radius: 1rem;
    background-color: color-mix(
      in oklab,
      var(--color-base-200) 70%,
      transparent
    );
    border: 1px solid
      color-mix(in oklab, var(--color-base-content) 8%, transparent);
    transition:
      transform 0.2s ease,
      border-color 0.2s ease,
      box-shadow 0.2s ease,
      background-color 0.2s ease;
  }
  .card-link:hover .card-h {
    transform: translateY(-2px);
    border-color: color-mix(in oklab, var(--color-primary) 40%, transparent);
    box-shadow: 0 20px 40px -20px rgba(0, 0, 0, 0.4);
    background-color: var(--color-base-200);
  }
  .card-h-media {
    flex-shrink: 0;
    width: 100%;
    overflow: hidden;
    border-radius: 0.75rem;
  }
  .card-h-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.3s ease;
  }
  .card-link:hover .card-h-img {
    transform: scale(1.03);
  }
  .card-h-body {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    min-width: 0;
    flex: 1;
  }
  .card-h-head {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-wrap: wrap;
  }
  .card-h-title {
    margin: 0;
    font-size: 1.25rem;
    font-weight: 700;
    line-height: 1.3;
  }
  .card-link:hover .card-h-title {
    color: var(--color-primary);
  }
  .card-h-badge {
    font-size: 0.7rem;
    font-weight: 600;
    padding: 0.2rem 0.55rem;
    border-radius: 9999px;
    background-color: var(--color-secondary);
    color: var(--color-secondary-content);
  }
  .card-h-desc {
    margin: 0;
    color: color-mix(in oklab, var(--color-base-content) 75%, transparent);
    line-height: 1.5;
    font-size: 0.95rem;
  }
  .card-h-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem;
    margin: 0.25rem 0 0;
    padding: 0;
    list-style: none;
  }
  .card-h-tags li {
    font-size: 0.75rem;
    padding: 0.2rem 0.6rem;
    border-radius: 9999px;
    background-color: color-mix(
      in oklab,
      var(--color-base-300) 60%,
      transparent
    );
    color: color-mix(in oklab, var(--color-base-content) 75%, transparent);
    border: 1px solid
      color-mix(in oklab, var(--color-base-content) 8%, transparent);
  }

  @media (min-width: 640px) {
    .card-h {
      flex-direction: row;
      align-items: stretch;
      gap: 1.25rem;
    }
    .card-h-media {
      width: 13rem;
      aspect-ratio: 16 / 10;
    }
  }
</style>
